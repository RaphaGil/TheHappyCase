# API routes must come BEFORE the catch-all SPA rule
# Rewrite API routes to Netlify Functions (200 = rewrite, not redirect)
/api/create-payment-intent    /.netlify/functions/create-payment-intent    200
/api/inventory                /.netlify/functions/inventory                 200

# SPA catch-all - must be last
/*    /index.html   200