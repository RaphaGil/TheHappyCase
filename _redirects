# Serve SEO files directly
/robots.txt   /robots.txt   200!
/sitemap.xml  /sitemap.xml  200!

# SEO-friendly URL redirects (301 = permanent, passes link equity)
/CreateYours     /custom-passport-holder    301!
/CreateYours/    /custom-passport-holder    301!

# API routes → Netlify Functions
/api/create-payment-intent    /.netlify/functions/create-payment-intent    200
/api/inventory                /.netlify/functions/inventory                 200
/api/save-order               /.netlify/functions/save-order                200

# Note: No SPA fallback needed - Next.js static export generates HTML files for each route
# The netlify.toml redirects handle the routing correctly
