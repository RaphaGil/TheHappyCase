# Copy to .env and fill in your values.
# For Netlify: set these in Site settings → Environment variables (SUPABASE_* required for orders to save).

# --- Supabase (required for saving orders) ---
# Get from Supabase: Project settings → API → Project URL and service_role key
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --- Stripe ---
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...

# --- Resend (optional; for order confirmation + dispatch/tracking emails) ---
# Get API key from https://resend.com/api-keys. Same key used for both:
# - Order confirmation email (after payment)
# - Dispatch notification email ("Your pack is on its way" when you mark order dispatched)
# RESEND_API_KEY=re_xxxx
# FROM_EMAIL=onboarding@resend.dev

# --- Optional ---
# BACKEND_API_URL=  # Only set if you want save-order to proxy to another backend instead of Supabase
