export const metadata = {
  title: 'Cart | The Happy Case',
  description: 'Review your selected custom passport cases before checkout.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function CartLayout({ children }) {
  return children;
}
